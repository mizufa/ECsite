<!DOCTYPE html>
<html>
  <head>
    <title>ECsite</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

 <header>
    <nav class="navbar navbar-expand-lg border-bottom border-dark">
      <div class="container">
        <% if admin_signed_in? %>
        <li>
          <%= link_to "ログアウト", destroy_admin_session_path, method: :delete %>
        </li>
        <% else %>
         <div class="row">
        <li class="list-unstyled">
          <h1>LOGO</h1>
        </li>
        </div>

        <div class="row">
        <ul class="navbar-nav pull-right">
          <button type="button" class="btn btn-outline-secondary">
            <%= link_to "About",home_about_path, class: "text-dark" %>
          </button>
          <button type="button" class="btn btn-outline-secondary">
            <%= link_to "商品一覧",home_about_path, class: "text-dark" %>
          </button>
          <button type="button" class="btn btn-outline-secondary">
            <%= link_to "新規登録",home_about_path, class: "text-dark" %>
          </button>
          <button type="button" class="btn btn-outline-secondary">
            <%= link_to "ログイン", new_admin_session_path, class: "text-dark" %>
          </button>
        </ul>
        </div>
        <% end %>
        </div>
      </div>
    </nav>
  </header>

  <body>
    <%= yield %>
  </body>

      <footer>
      <div class="container mt-5 border-top border-dark"></div>
      </footer>
</html>
