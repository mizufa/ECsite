<h1>Public::Items#show</h1>

<div class="container">
  <div class="row">
    <div class="col">
      <%= image_tag @item.get_image %>
    </div>
    <div class="col">
      <p><%= @item.name %></p>
      <p><%= @item.introduction %></p>
      <p>￥<%= @item.purchase_price %>(税込)</p>

    <%= form_with model: @cart_item do |f| %>
    <span>
      <%= f.select :amount, *[1..10] %>
      <%= f.hidden_field :item_id, :value => @item.id %>
    </span>
    <span>
      <%= f.submit 'カートに入れる' %>
    </span>
    <% end %>

    </div>
  </div>

</div>